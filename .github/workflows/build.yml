name: Firmware Build

on:
  push:
    paths-ignore:
      - "keymap/**"
  pull_request:
  workflow_dispatch:

jobs:
  firmware:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3.0
